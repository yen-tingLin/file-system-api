/**
 * Absolute path to our app directory
 */
// const absPath = path.dirname(require.main.filename);

/**
 * Absolute path to our app directory
 */
// global.__basedir = __dirname;
// console.log(global.__basedir);

//==========================

// fs.readFile("./files/test", "utf8", (err, data) => {
//   console.log(data);
// });

/**
 * Enum for OrderBy
 * @param {*} orderBy
 */
// function showOrderBy(orderBy) {
//   if (!(orderBy in OrderBy)) {
//     console.log("not exist !");
//   }

//   switch (orderBy) {
//     case OrderBy.lastModified:
//       console.log("lastModified !!");
//       break;
//     case OrderBy.size:
//       console.log("size !!");
//       break;
//     case OrderBy.fileName:
//       console.log("fileName !!");
//       break;
//   }
// }

// showOrderBy(OrderBy.lastModified);

/**
 * read single file
 */
//   fs.readFile("." + filePath, "utf8", (err, data) => {
//     console.log(data);
//   });
